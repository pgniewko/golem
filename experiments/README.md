# Experiments

`experiments/pretrain` in the main README is an example output path for fresh runs.

The repository does not ship a live baseline in that location anymore. Any tracked historical artifacts are archived under [`experiments/archive`](./archive) and should not be treated as current training outputs.
