# Public pretrained model

`best_checkpoint.pt` was pretrained with:

- **gt-pyg 1.6.5**
- **golem** `0.1.2.dev10+72646b8`

Load it with *gt-pyg 1.6.5* to reproduce the model.

See `resolve_config.yaml` for the full training config.
